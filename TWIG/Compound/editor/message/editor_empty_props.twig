{# ... #}

{% macro render(event) %}

    {% import 'TWIG/Prototypes/message/compound_message.twig' as prototype %}

    {{ prototype.render({
        icon: 'dashicons-warning',
        title: 'Component has no parameters',
        description: 'This component has no parameters, so you cannot change it.',
        class: '',
        attr: {}
    }) }}

{% endmacro %}