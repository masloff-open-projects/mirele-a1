{% macro render(event) %}

    <div id="modal_edit_template" style="display:none;">
        <div class="Compound">

            {# Modal content #}
            <div class="CModal">
                <form action="" method="post" id="modal_edit_template_form" @submit.prevent="submit" class="left">

                    <div class="field-box">
                        <div class="field-title-box">
                            <h4 class="title">Lorem ipsum dolor sit.</h4>
                            <small class="small">Lorem ipsum dolor sit.</small>
                        </div>

                        <div>
                            <input type="text" value="{{ event.page.ID }}" class="" readonly>
                        </div>
                    </div>

                </form>
            </div>
        </div>
    </div>

{% endmacro %}