{# Orders Block #}

<div class="blocks px-2 mb-3">
    <div class="container p-1">
        <div class="row blocks orders">
            <div class="col-md-12">
                {% if account.orders|length > 0 %}
                    {{ Component('default_orders_table', []) }}
                {% else %}
                    {{ Component('default_orders_placeholder', []) }}
                {% endif %}
            </div>
        </div>
    </div>
</div>